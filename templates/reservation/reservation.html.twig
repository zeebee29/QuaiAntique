{% extends 'base_reservation.html.twig' %}

{% block body %}
<body>
    <div class="container">
        <h1 class="title-page-restau">RESERVATION</h1>
    {% if app.user %}
        <form action="{{ path('reservation1',{id:app.user.id}) }}" method="POST">
    {% else %}
        <form action="{{ path('reservation1') }}" method="POST">
    {% endif %}                        

            {{ form_start(form) }}

        <div class="container-resa">
            <div class="d-flex justify-content-center">
                <div class="col-md-6 mb-2 mt-2">
                    <h2 class="titre-page-resa">Combien serez-vous ?</h2>
                    <p>(à partir de 10 personnes, nous ne prenons les réservations que par téléphone)</p>
                </div>
            </div>
            <div class="d-flex mt-4 mb-5 justify-content-center">
                <div>
                            {{ form_widget(form.nbConvive) }}
                </div>
            </div>
            <div class="d-flex mb-5 justify-content-center resa-row-btn">
                <a href="{{ path('homepage') }}" role="button" class="btn btn-secondary m-3">Annuler</a>
                <button type="submit" class="btn btn-primary m-3">Date >></button>
            </div>
        </div>
        </div>
    </div>
    {{ form_end(form) }}
</form>
</body>

{% endblock %}