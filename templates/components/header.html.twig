<header class="header">
<div class="container-fluid px-0">
<nav class="navbar navbar-expand-md navbar-dark bg-dark px-2 fixed-top">
    <div class="navbar2">
        <div class="navbar1">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse navbar-all" id="navbarNavDropdown">
                <div class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('homepage') }}">Accueil<span class="sr-only"></span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link no-break" href="{{ path('app_restau') }}">Le restaurant</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_plats') }}">Les plats</a>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_carte') }}">La carte</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_menu') }}">Les menus</a>
                    </li>
                    <li class="nav-item">
                            <a class="nav-link {%if pageR is defined %} active {% endif %}"
                        {% if app.user %}
                            href="{{ path('reservation1', {id:app.user.id}) }}"
                        {% else %}
                            href="{{ path('reservation1') }}"
                        {% endif %}                        
                        >Réservation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('app_contact') }}">Nous contacter</a>
                    </li>
                </div>
            </div>
        </div>
        <div>
            <a class="navbar-brand text-light" href="{{ path('homepage') }}">
                <img class="logo-navbar" src="{{ asset('images/LogoRestaurant.svg')}}" alt="Logo Restaurant" />
            </a>
            </div>
        <div class="navbar-brand nav-user">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-user2 nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-bs-toggle="dropdown"
                        aria-expanded="true">
                        <i class="fa fa-user"></i>
                {% if app.user %}
                        <span class="txt-welcome-navbar">{{ app.user.nom }} </span>
                    </a>
                    <div class="position-absolute dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('user-booking', {id:app.user.id}) }}">Historique de mes réservations</a>
                        <a class="dropdown-item" href="{{ path('user.edit', {id:app.user.id}) }}">Modifier mes
                            informations</a>
                        <a class="dropdown-item" href="{{ path('user-edit-passsword', {id:app.user.id}) }}">Modifier mon
                            mot de passe</a>
                        <div class="dropdown-divider"></div>
                        {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="{{ path('admin') }}">Espace ADMIN</a>
                        {% endif %}
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
                    </div>
                </li>
                {% else %}
                <span class="txt-welcome-navbar">Mon compte</span>
            </a>
                    <div class="position-absolute dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('security.login') }}">Me connecter</a>
                        <a class="dropdown-item" href="{{ path('security.registration') }}">M'inscrire</a>
                    </div>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
</div>
</header>
<script>
    let prevScrollPos = window.pageYOffset;

window.onscroll = function() {
    let currentScrollPos = window.pageYOffset;
    
    if (prevScrollPos > currentScrollPos) {
        document.querySelector(".navbar").style.top = "0";
    } else {
        document.querySelector(".navbar").style.top = "-100px";
    }
    
    prevScrollPos = currentScrollPos;
}

</script>